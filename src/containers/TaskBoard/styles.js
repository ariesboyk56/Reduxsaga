const styles = () => ({
  board: {
    display: "flex",
    alignItems: "center"
  },
  shape: {
    padding: 20,
    margin: 10,
    backgroundColor: "#333",
    color: "#fff",
    borderRadius: 4,
    borderColor: "#fff"
  }
});

export default styles;